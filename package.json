{
  "name": "sampleElectronApp",
  "version": "1.0.0",
  "description": "A sample Electron application",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . Application --platform=win32 --arch=x64 --overwrite --prune=true --out=build --asar --version-string.ProductName=\"Sample Electron App\""
  },
  "repository": "https://github.com/Romanow88/electron-sample-app",
  "devDependencies": {
    "electron": "~1.7.8",
    "electron-packager": "^9.1.0"
  }
}
